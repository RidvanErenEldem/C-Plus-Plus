#include <iostream>

using namespace std;

main()
{
	int x = 10, y = 3;
	
	cout << "x == y " << (x==y);
	cout << "\nx != y " << (x!=y);
	cout << "\nx<y " << (x<y);
	cout << "\nx>y " << (x>y);
	cout << "\nx<=y " << (x<=y);
	cout << "\nx>=y " << (x>=y);
	//if it is true then return 1 or else 0
}
